import { Button, VerticalBox } from "std-widgets.slint";
import { Router,CurrentPage } from "./global.slint";

export component SheetInsert inherits VerticalBox {
    HorizontalLayout {
        height: 10%;
        Button {
            width: 5%;
            text: "<-";
            clicked => {
                Router.page = CurrentPage.SheetHome
            }
        }
    }
}
