use std::fs::File;
use std::io::Write;

fn main(){
    let b = serde_json::to_string(&models::get_sheet()).unwrap_or_default();
    
    let mut file = File::create("output.json").unwrap();

    file.write_all(b.as_bytes()).unwrap();
}
